module PP

imports common/Identifiers	
        terms/Terms


context-free syntax

  PropPattern.HasTypePat = <<Pattern> : <Term>> {avoid}
  PropPattern.HasPat = <<Pattern> has <Id> <Term>> {avoid}
  PropPattern.IsWF = <<Pattern> :-> {avoid}
