module PP

imports

	common/Identifiers
	terms/Terms

templates // Productions for desugared patterns, used to pretty print desugared code.

  PropPattern.HasTypePat = <<Pattern> : <Term>> {avoid}
  PropPattern.HasPat     = <<Pattern> has <Id> <Term>> {avoid}
  PropPattern.IsWF       = <<Pattern> :-> {avoid}
